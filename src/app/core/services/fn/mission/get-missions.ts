/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageResponseMissionResponse } from '../../models/page-response-mission-response';

export interface GetMissions$Params {
  title?: string;
  page?: number;
  size?: number;
}

export function getMissions(http: HttpClient, rootUrl: string, params?: GetMissions$Params, context?: HttpContext): Observable<StrictHttpResponse<PageResponseMissionResponse>> {
  const rb = new RequestBuilder(rootUrl, getMissions.PATH, 'get');
  if (params) {
    rb.query('title', params.title, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageResponseMissionResponse>;
    })
  );
}

getMissions.PATH = '/api/missions';
