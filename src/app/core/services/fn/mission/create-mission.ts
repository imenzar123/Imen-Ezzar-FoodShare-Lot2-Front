/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MissionRequest } from '../../models/mission-request';
import { MissionResponse } from '../../models/mission-response';

export interface CreateMission$Params {
      body: MissionRequest
}

export function createMission(http: HttpClient, rootUrl: string, params: CreateMission$Params, context?: HttpContext): Observable<StrictHttpResponse<MissionResponse>> {
  const rb = new RequestBuilder(rootUrl, createMission.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MissionResponse>;
    })
  );
}

createMission.PATH = '/api/missions';
